# CLAUDE.md

> This file is intentionally minimal. **Fleshing it out is Exercise 1** of the
> workshop (see `docs/WORKSHOP.md`). Replace this note with real project context.

## What this is

A small Express REST API for managing tasks. In-memory data store, no database.

## TODO (Exercise 1)

Document the things Claude should know on every session, for example:

- The tech stack and Node version
- How to run, test, lint, and format the project
- The folder structure and the "resource" pattern under `src/resources/`
- The definition of done for any change (validation + tests must pass)
- Conventions Claude should always follow (and things it should never do)
